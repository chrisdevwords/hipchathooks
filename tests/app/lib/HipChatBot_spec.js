var should = require('should'),
    sinon = require('sinon'),
    request = require('request'),
    GenericBot = require('../../../app/lib/HipChatBot');

describe('GenericBot', function () {
    it('should be able to parse a HipChat Request', function (done) {
        done();
    });
});
